<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HouseKeeperApplication"
        tools:targetApi="31">
        <activity
            android:name=".profile.EditHousekeeperProfile"
            android:exported="false" />
        <activity
            android:name=".profile.EditFamilyProfile"
            android:exported="false" />
        <!--
        <activity
            android:name=".profile.HousekeeperProfile"
            android:exported="false" />
        -->
        <activity
            android:name=".profile.FamilyProfile"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:exported="false" />
        <!--
<activity
            android:name=".register"
            android:exported="false"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.HouseKeeperApplication" />
        -->
        <activity
            android:name=".login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".profile.HousekeeperProfile"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>